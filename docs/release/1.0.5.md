# ApiCat 1.0.5 Release Notes

**发布日期**：2026-09-22（收起追加批次后的收口日；tag 与早期批次见 09-19）
**版本类型**：Stable Feature Release

---

## 关于此版本

ApiCat 1.0.5 的核心是 **Localhost HTTP Bridge + AI 断言引擎 + 技能安装器**，把 ApiCat 从「桌面调试工具」扩展为「可被 AI 技能直接驱动的本地数据服务」，并补齐了压测报告的真源下沉与接口文档面板。

Bridge 在本地 17320 端口起一个 REST 服务（启动生成随机 token 写入 `bridge.json`），所有技能（apicat-lib / apicat-test-gen / apicat-doc-gen）经 Bridge 读写接口、字典、用例、压测 —— 数据真源收敛到一处，技能脚本不再直连 SQLite。断言模块让测试用例可声明「期望状态码 / JSON Path 校验」，发送后给出通过/失败判定。技能安装器一键把技能装进各 Agent 的 skills 目录，App 更新后自动修复悬空的 junction 链接。

配套：压测报告 Markdown 生成器下沉到 Rust（App 与技能共用同一真源），报告预览弹窗支持渲染与 md/html 导出；侧边栏新增「接口文档」面板。

---

## 核心更新

| 功能 | 说明 |
|------|------|
| Localhost HTTP Bridge | `127.0.0.1:17320` 起 REST 服务，启动生成随机 token；GET=只读 / POST=写，路径对齐 IPC 命令名；写操作后广播 `bridge-data-changed`，前端 store 自动刷新 |
| AI 技能安装器 | 一键安装/卸载 4 个内置技能（apicat-lib / apicat-edit / apicat-test-gen / apicat-doc-gen）到各 Agent skills 目录（junction `/J`，无需管理员权限）；App 更新后自动检测并修复悬空链接 |
| 断言模块 | 测试用例支持声明断言 JSON（`status_code` / `json_path`），运行结果给出通过/失败判定；断言编辑与结果 UX 完善 |
| 压测报告真源下沉 | Markdown 报告生成器移到 Rust（App 与技能共用），`get_stress_report` IPC + `GET /stress_report` 端点统一取数 |
| 报告预览弹窗 | 压测结果可直接预览渲染后的报告，支持 Markdown / HTML 导出 |
| 接口文档面板 | 侧边栏第三个 Tab：浏览本地文档目录，双击打开文件 / 在文件管理器中定位 |
| 压测增强 | 期望状态码断言、响应率/业务成功率双指标卡片、对比图改多指标分组双柱 + 对比表 |
| 压测参考线可配 | P95/P99 参考线不再写死 500/1000ms：项目级默认 + 接口级覆盖，压测配置弹窗可改；生效值随运行快照进 `config_json`，历史报告不受后续改动影响 |
| 主题取色升级 | Token 取色器换用 NaiveUI `n-color-picker`，支持 rgba/alpha 通道编辑；预设主题应用时同步切换深浅模式（可再独立切回） |
| 响应字段字典翻译 | 自研 JSON 树替换 vue-json-pretty；响应字段值命中字典项时，值旁显示 `value ▸ 含义` 字典色微标签（只读翻译，hover 看字典名 + 描述） |
| 接口编辑撤销/重做 | 结构级 undo/redo（加删行/排序/清空/粘贴 + URL/method，栈深 20，按接口分栈）；编辑区「⚠ 未保存」标记 + 一键回到上次 Ctrl+S 版本 |
| 历史记录增强 | `test_case_history` 并入 `request_history`；点击历史行行内预览响应（对齐实时响应渲染）；大响应文件存储 + 另存为；历史清理 |
| 字典管理改版 | 管理页双 tab；左侧栏两行化（字典 code 主 / name 弱化）+ 字典项描述 ⓘ 悬停；绑定下拉按 code 升序 + 可搜索 |
| 左侧栏折叠 | 接口/字典/文档三栏统一的折叠按钮，折叠后右侧全宽，状态持久化 |
| 用例管理改进 | 按最近使用排序、默认不激活 + Send 落点（口径乙）、移除收藏、再次点击已激活用例退回接口编辑区 |
| 文档预览增强 | 文档面板内 Markdown 预览 + 滚动失效修复（滚轮 + 可拖拽滚动条）+ 非 md 类型防御占位 |

---

## 重点功能说明

### 🔌 HTTP Bridge

- 监听 `127.0.0.1`，端口 17320 起、占用顺延；token 写入 `~/.apicat/bridge.json`，技能通过 `Authorization: Bearer <token>` 访问；
- 协议：REST，前缀 `/api/v1/`，路径与 IPC command 名对齐；GET=只读（query 参数），POST=写（JSON body）；
- 写操作后广播 `bridge-data-changed` 事件，前端各 store 自行 reload —— 技能改完数据，桌面 UI 即时反映；
- Bridge 开关存 `app-settings.json`（默认开启），无需重装依赖。

### 🤖 AI 技能体系

4 个内置技能收拢在 `skills/` 目录，安装器一键装进各 Agent 的 skills 目录（junction `mklink /J`，免管理员权限）；App 更新后启动时自动扫描修复悬空链接。所有技能只经 HTTP Bridge 读写数据，**禁止直连 SQLite**（直写会绕过 UI 刷新、业务校验与断言引擎）：

| 技能 | 职责 | 触发词示例 |
|------|------|-----------|
| `apicat-lib` | 库访问底座：定位 Bridge（读 `bridge.json`）、鉴权、curl 约定、共享约束；其余技能都以它为前置 | 「看看 apicat 里有什么」 |
| `apicat-edit` | 数据编辑：增删改接口/数据字典/字段绑定；从 Markdown 接口文档批量导入接口（AI 直读文档提取，先过目再落库） | 「加接口」「新建字典」「从文档导入」 |
| `apicat-test-gen` | 为接口生成 7 种类型测试用例（可从定义/文档/源码推断参数），写入后可跑断言、可压测 | 「生成测试用例」「压测」 |
| `apicat-doc-gen` | 读接口定义 + 用例，生成标准 Markdown 接口文档（富字段表 + 多场景 Response），输出目录可在项目设置里指定 | 「生成接口文档」 |

### ✅ 断言模块

- 用例可附加断言：`{"type":"status_code","operator":"eq","expected":"200"}` 与 `{"type":"json_path","path":"$.code","operator":"eq","expected":"0"}`；
- 断言求值引擎在 Rust 侧（`assertion` 模块），发送请求后对响应做校验并给出判定；断言编辑交互完善。

### 📏 压测参考线

- 阈值两级：项目级默认（`projects.p95_threshold_ms` / `p99_threshold_ms`，默认 500 / 1000）+ 接口级覆盖（`api_requests` 同名列，空 = 继承项目级）；
- 压测配置弹窗可直接修改本次运行的参考线；启动压测时把生效值**快照写入** `stress_runs.config_json`，报告结论与配置表只读快照 → 后续改阈值不影响历史报告，旧记录无此字段时自动回退默认值；
- 报告 Markdown 的结论摘要与「六、附录·压测配置」同步展示所用参考线；Bridge 的 `start_stress` 端点透传同一参数。

---

## 重要修复

- 4 项阻塞/高优问题修复（code review 后）：技能触发、相对 URL 处理、断言日志、断言 UX
- 中文 body 必须走 heredoc/文件传输 —— 内联会被 Git Bash 转码（技能脚本）
- 接口查重改为 method + url，避免从文档导入出重复项
- junction 路径分隔符混用（正斜杠）导致 mklink 失败的隐患
- 文档「打开所在目录」打开 C 盘、用例压测 `{{base_url}}` 未替换
- 文档面板按钮误用 info 蓝、TestCaseManager 悬停兜底色改主题绿
- 字典项目隔离、复制字典 UNIQUE 约束等 4 个问题
- `{{base_url}}` 占位符不再被前端拼接破坏（真实值替换后前端会再拼一段路径）
- 参数行输入失焦（rowKey 用 WeakMap 稳定身份）+ URL 高亮光标错位
- 跑用例误写历史违约、Body 类型切换误报草稿态、历史预览改回抽屉
- 字典树 h() 渲染吃不到 scoped 样式、手写悬停内容浅色主题下不可见
- 字典全局泄漏堵漏：导入字典归属性项目、Bridge 建字典 projectId 必填、`loadFieldBindings` 防竞态

---

## 工程与维护改进

- **types 镜像漂移对齐**：`types.rs ⇄ types/index.ts` 两处字段对齐
- **P-series 后端清理**：列清单常量统一到 `sql_cols`、bridge 双键取值收敛为 `get_str/get_i64`、写操作抽 `_impl` 收敛 bridge 与 commands 业务分叉、消除 N+1 IPC 与历史列表全量拉 body
- **写操作真源进一步收敛**：项目 CRUD 抽到 `project.rs` impl，Bridge 与 IPC 共用一份 SQL；前端字段绑定/字典加载防竞态
- **技能脚本精简**：去 jq/node 依赖（零安装门槛），只留 bridge_client，推理活交回 AI；app 启动自动修复悬空 skill junction 链接
- **前端去冗余**：拖拽排序逻辑抽为 `useCollectionTreeDnd` composition；`test_case_history` 合并入 `request_history`；清死代码
- **回归脚本修正**：`repro-draft-pollution.mjs` 按 1.0.4 真实 bug（防抖落库 id 漂移）重写，闭环成立（旧 50/50 污染、新 0/50）
- **CI**：macOS 打包补 `app` bundle，修复 `latest.json` 缺失 `darwin-*` 条目（Mac 收不到更新的问题）；记录发布流程四个坑

---

## 兼容与升级

- Migration 幂等，老库无需清空；Bridge/断言均为新增能力，不影响既有接口、用例、字典数据；
- 技能目录经原生资源打包，升级后 skills/ 由 App 自动维护，无需手动重建链接；
- 建议升级：需要在别的工具/AI 助手驱动 ApiCat 数据、或要更严谨的用例断言与报告时，收益最直接。

---

## 版本范围

本说明覆盖以下提交范围：

- From: `v1.0.4`
- To: `a86b09c`（含 tag 之后并入 1.0.5 的提交：压测参考线可配、主题取色 rgba、若干 UI 修正，以及 09-21~22 追加批次——响应字段字典翻译、接口编辑撤销重做、历史记录增强、字典管理改版、左侧栏折叠等，全部并入本说明）