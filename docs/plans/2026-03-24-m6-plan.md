# M6 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 实现压测引擎（tokio 并发 + 实时折线图）和导入导出（Postman Collection v2.1 + OpenAPI 3.x），完成 ApiCat v1.0 功能闭环。

**Architecture:**
- **压测**：Rust `commands/stress.rs`，tokio spawn 并发发请求，共享 `Arc<Mutex<StressStats>>` 聚合统计，独立定时任务每 200ms emit 一次 `stress://progress` 事件（§10.1 CRITICAL：禁止逐请求 emit）；前端 `StressConfigModal.vue` + `StressResultPanel.vue`（canvas 折线图）。
- **导入导出**：Rust `commands/io.rs`，解析 Postman v2.1 JSON 和 OpenAPI 3.x YAML/JSON，写入 SQLite；导出反向序列化当前项目；Cargo 新增 `serde_yaml`；使用 `tauri-plugin-dialog` 做文件选择/保存对话框；前端 `ImportDialog.vue` + `ExportDialog.vue`。
- **入口**：压测按钮加在 MainPanel.vue URL 栏旁；导入导出挂在 TopBar.vue 的「⚙️」按钮弹出菜单。

**Tech Stack:** Tauri 2.x · Rust · tokio · reqwest · serde_yaml · SQLite(sqlx 0.8) · Vue 3 · TypeScript · Naive UI · Pinia

---

## 总览

| Task | 内容 | 文件 |
|------|------|------|
| T1 | Cargo.toml 新增依赖 + tauri.conf.json 注册插件 | 改 Cargo.toml、tauri.conf.json、src-tauri/src/lib.rs |
| T2 | Rust：压测引擎核心（tokio 并发 + 聚合推送） | 新建 commands/stress.rs，改 commands/mod.rs、lib.rs |
| T3 | 前端：stress store + 类型定义 | 新建 stores/stress.ts，改 types/index.ts |
| T4 | 前端：StressConfigModal.vue（配置弹窗） | 新建 components/stress/StressConfigModal.vue |
| T5 | 前端：StressResultPanel.vue（实时折线图） | 新建 components/stress/StressResultPanel.vue |
| T6 | MainPanel.vue 集成压测按钮 + 弹窗 | 改 components/layout/MainPanel.vue |
| T7 | Rust：导出（ApiCat 格式 + Postman v2.1） | 新建 commands/io.rs（导出部分），改 commands/mod.rs、lib.rs |
| T8 | Rust：导入（Postman v2.1 + OpenAPI 3.x） | 改 commands/io.rs（导入部分） |
| T9 | 前端：ImportDialog.vue + ExportDialog.vue | 新建 components/io/ 目录下两个组件 |
| T10 | TopBar.vue 集成导入导出入口 | 改 components/layout/TopBar.vue |

---

## 关键约束（实现时必须遵守）

1. **§10.1 CRITICAL — 禁止逐请求 emit**：压测时只能每 200ms 聚合推送一次，绝对不允许每请求 emit。
2. **§10.2 HIGH — fd 上限检测**：压测启动前在 Rust 层检测可用 fd 数（Windows 跳过，macOS/Linux 检测 `getrlimit`），不足 `max_concurrent * 4` 时返回 error，由前端展示警告。
3. **reqwest 连接池**：`reqwest::Client` 构建时 `.pool_max_idle_per_host(max_concurrent as usize)` 限制连接数。
4. **HTTP 请求必须在 Rust 层发送**：导入后验证不调任何前端 fetch。
5. **每次 commit 原子化**：每个 Task 完成后独立 commit，不批量。

---

## Task 1：Cargo.toml 新增依赖 + 插件注册

**文件：**
- 修改：`src-tauri/Cargo.toml`
- 修改：`src-tauri/tauri.conf.json`
- 修改：`src-tauri/src/lib.rs`（注册 tauri-plugin-dialog 和 tauri-plugin-fs）

### Step 1：在 `Cargo.toml` 的 `[dependencies]` 末尾追加

```toml
# YAML 解析（用于 OpenAPI 3.x .yaml 导入）
serde_yaml = "0.9"

# Tauri 官方文件对话框插件（用于文件选择/保存）
tauri-plugin-dialog = "2"

# Tauri 官方文件系统插件（用于读写文件）
tauri-plugin-fs = "2"
```

### Step 2：在 `src-tauri/tauri.conf.json` 的 `"plugins"` 中添加权限

找到 `tauri.conf.json` 中的 `"app"` → `"security"` → `"capabilities"` 区块（或根层级的 `"plugins"`），加入：

> ⚠️ Tauri 2.x 的权限系统通过 `capabilities/` 目录配置，检查是否存在 `src-tauri/capabilities/default.json`，若存在则在其中添加；若不存在则创建。

检查：
```bash
ls src-tauri/capabilities/
```

若 `default.json` 已存在，在其 `"permissions"` 数组追加：
```json
"dialog:default",
"fs:default"
```

若不存在 `capabilities/` 目录，则创建 `src-tauri/capabilities/default.json`：
```json
{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default"
  ]
}
```

### Step 3：在 `src-tauri/src/lib.rs` 中注册插件

在 `Builder::default()` 链中，其他 `.plugin(...)` 之后添加：
```rust
.plugin(tauri_plugin_dialog::init())
.plugin(tauri_plugin_fs::init())
```

### Step 4：cargo check

```powershell
powershell -NonInteractive -Command '
$vsdev = "C:\Program Files\Microsoft Visual Studio\2022\Community\Common7\Tools\VsDevCmd.bat"
$psi = New-Object System.Diagnostics.ProcessStartInfo("cmd.exe")
$psi.Arguments = "/c `"$vsdev`" && cargo check 2>&1"
$psi.UseShellExecute = $false
$psi.RedirectStandardOutput = $true
$psi.RedirectStandardError = $true
$psi.WorkingDirectory = "D:\ProgramFiles\rangeric\apicat\src-tauri"
$p = [System.Diagnostics.Process]::Start($psi)
$out = $p.StandardOutput.ReadToEnd()
$err = $p.StandardError.ReadToEnd()
$p.WaitForExit()
Write-Output $out; Write-Output $err; Write-Output "EXIT: $($p.ExitCode)"
'
# 期望：EXIT: 0（serde_yaml、tauri-plugin-dialog、tauri-plugin-fs 下载并编译通过）
```

### Step 5：提交

```bash
git add src-tauri/Cargo.toml src-tauri/src/lib.rs
git add src-tauri/capabilities/ 2>/dev/null || true
git commit -m "feat(m6): add serde_yaml + tauri-plugin-dialog/fs dependencies"
```

---

## Task 2：Rust — 压测引擎核心

**文件：**
- 新建：`src-tauri/src/commands/stress.rs`
- 修改：`src-tauri/src/commands/mod.rs`
- 修改：`src-tauri/src/lib.rs`

### Step 1：新建 `src-tauri/src/commands/stress.rs`

```rust
//! 压测引擎
//!
//! 关键约束（§10.1 CRITICAL）：
//!   - 禁止逐请求 emit；每 200ms 聚合一次统计后推送
//!   - reqwest 连接池大小不超过 max_concurrent（§10.2）
//!   - Windows 跳过 fd 检测；macOS/Linux 检测 getrlimit

use crate::{db::AppDb, error::CmdResult, http::client::{send, SendRequestParams}};
use serde::{Deserialize, Serialize};
use std::{
    sync::{Arc, Mutex},
    time::{Duration, Instant},
};
use tauri::{AppHandle, Emitter, State};
use tokio::sync::broadcast;

// ── 压测配置 ──────────────────────────────────────────────

/// 压测模式
#[derive(Debug, Deserialize, Clone)]
#[serde(rename_all = "lowercase")]
pub enum StressMode {
    /// 固定总请求数
    Count,
    /// 持续时间（秒）
    Duration,
}

// ── 统计数据（每 200ms 快照推送到前端）────────────────────

#[derive(Debug, Default, Clone, Serialize)]
pub struct StressStats {
    pub total: u64,
    pub success: u64,
    pub failed: u64,
    pub success_rate: f64,    // 0.0 ~ 100.0
    pub avg_ms: f64,
    pub p50_ms: u64,
    pub p95_ms: u64,
    pub p99_ms: u64,
    pub tps: f64,
    pub elapsed_sec: f64,
    pub done: bool,           // 压测是否已结束
}

/// 内部可变统计（由 worker tasks 写入）
#[derive(Default)]
struct RawStats {
    total: u64,
    success: u64,
    failed: u64,
    durations_ms: Vec<u64>,   // 所有请求耗时，用于百分位计算
}

impl RawStats {
    fn record(&mut self, ok: bool, duration_ms: u64) {
        self.total += 1;
        if ok { self.success += 1; } else { self.failed += 1; }
        self.durations_ms.push(duration_ms);
    }

    fn snapshot(&self, elapsed_sec: f64, done: bool) -> StressStats {
        let mut sorted = self.durations_ms.clone();
        sorted.sort_unstable();
        let n = sorted.len();

        let percentile = |pct: f64| -> u64 {
            if n == 0 { return 0; }
            let idx = ((n as f64 * pct / 100.0).ceil() as usize).saturating_sub(1).min(n - 1);
            sorted[idx]
        };

        let avg_ms = if n == 0 {
            0.0
        } else {
            sorted.iter().sum::<u64>() as f64 / n as f64
        };

        let success_rate = if self.total == 0 {
            0.0
        } else {
            self.success as f64 / self.total as f64 * 100.0
        };

        let tps = if elapsed_sec > 0.0 {
            self.total as f64 / elapsed_sec
        } else {
            0.0
        };

        StressStats {
            total: self.total,
            success: self.success,
            failed: self.failed,
            success_rate,
            avg_ms,
            p50_ms: percentile(50.0),
            p95_ms: percentile(95.0),
            p99_ms: percentile(99.0),
            tps,
            elapsed_sec,
            done,
        }
    }
}

// ── fd 上限检测（macOS / Linux）────────────────────────────

#[cfg(unix)]
fn check_fd_limit(max_concurrent: u32) -> Result<(), String> {
    use std::mem::MaybeUninit;

    // SAFETY: getrlimit 是标准 POSIX syscall，参数结构体符合规范
    let mut rlim: libc::rlimit = unsafe { MaybeUninit::zeroed().assume_init() };
    let ret = unsafe { libc::getrlimit(libc::RLIMIT_NOFILE, &mut rlim) };
    if ret != 0 {
        return Ok(()); // 获取失败时不阻断，保守通过
    }
    let current_limit = rlim.rlim_cur;
    let needed = (max_concurrent as u64) * 4;
    if current_limit != libc::RLIM_INFINITY && current_limit < needed {
        return Err(format!(
            "系统文件描述符上限（{}）不足以支撑 {} 并发（需要约 {}）。\
             请运行 `ulimit -n {}` 提升上限后重试。",
            current_limit, max_concurrent, needed, needed
        ));
    }
    Ok(())
}

#[cfg(not(unix))]
fn check_fd_limit(_max_concurrent: u32) -> Result<(), String> {
    Ok(()) // Windows 无此限制，直接通过
}

// ── 主压测 Command ─────────────────────────────────────────

/// 启动压测
///
/// # 参数
/// - `params`：复用 SendRequestParams（与普通发请求相同的请求配置）
/// - `concurrent`：并发 worker 数（1-500）
/// - `mode`：`"count"` | `"duration"`
/// - `value`：mode=count 时为总请求数；mode=duration 时为持续秒数
#[tauri::command]
pub async fn start_stress(
    app: AppHandle,
    params: SendRequestParams,
    concurrent: u32,
    mode: String,
    value: u64,
) -> CmdResult<()> {
    // ── 参数校验 ──────────────────────────────────────────
    if concurrent == 0 || concurrent > 500 {
        return Err(crate::error::AppError::Custom(
            "并发数必须在 1 ~ 500 之间".to_string(),
        ));
    }
    if value == 0 {
        return Err(crate::error::AppError::Custom(
            "请求数/持续时间不能为 0".to_string(),
        ));
    }

    // ── fd 上限检测（§10.2）──────────────────────────────
    check_fd_limit(concurrent).map_err(crate::error::AppError::Custom)?;

    // ── 构建专用 reqwest Client（连接池不超过 max_concurrent）
    let client = reqwest::Client::builder()
        .pool_max_idle_per_host(concurrent as usize)
        .timeout(Duration::from_secs(30))
        .danger_accept_invalid_certs(true)
        .build()
        .map_err(|e| crate::error::AppError::Custom(format!("构建 HTTP 客户端失败: {e}")))?;

    // ── 共享统计（Arc<Mutex<RawStats>>）──────────────────
    let raw_stats = Arc::new(Mutex::new(RawStats::default()));

    // ── 停止信号（broadcast channel）──────────────────────
    // 容量 1 足够，所有 worker 都会监听
    let (stop_tx, _) = broadcast::channel::<()>(1);

    let start_time = Instant::now();

    // ── 定时推送任务（每 200ms emit 一次，§10.1 聚合方案）─
    {
        let raw_stats_clone = Arc::clone(&raw_stats);
        let app_clone = app.clone();
        let mut stop_rx = stop_tx.subscribe();

        tokio::spawn(async move {
            let mut interval = tokio::time::interval(Duration::from_millis(200));
            loop {
                tokio::select! {
                    _ = interval.tick() => {
                        let snapshot = {
                            let guard = raw_stats_clone.lock().unwrap();
                            guard.snapshot(start_time.elapsed().as_secs_f64(), false)
                        };
                        // 推送进度事件（聚合后，非逐请求）
                        let _ = app_clone.emit("stress://progress", &snapshot);
                    }
                    _ = stop_rx.recv() => {
                        // 收到停止信号，发最终 done 快照后退出
                        let snapshot = {
                            let guard = raw_stats_clone.lock().unwrap();
                            guard.snapshot(start_time.elapsed().as_secs_f64(), true)
                        };
                        let _ = app_clone.emit("stress://progress", &snapshot);
                        let _ = app_clone.emit("stress://done", &snapshot);
                        break;
                    }
                }
            }
        });
    }

    // ── 压测 Worker 池 ────────────────────────────────────
    match mode.as_str() {
        "count" => {
            // 固定总请求数模式：用 semaphore 限并发，dispatch value 个任务
            let semaphore = Arc::new(tokio::sync::Semaphore::new(concurrent as usize));
            let mut handles = Vec::with_capacity(value as usize);

            for _ in 0..value {
                let permit = Arc::clone(&semaphore).acquire_owned().await
                    .map_err(|e| crate::error::AppError::Custom(format!("semaphore 错误: {e}")))?;
                let client_clone = client.clone();
                let params_clone = params.clone();
                let raw_clone = Arc::clone(&raw_stats);

                let h = tokio::spawn(async move {
                    let t = Instant::now();
                    let ok = send(&client_clone, &params_clone).await.is_ok();
                    let dur = t.elapsed().as_millis() as u64;
                    raw_clone.lock().unwrap().record(ok, dur);
                    drop(permit);
                });
                handles.push(h);
            }

            // 等待所有 worker 完成
            for h in handles {
                let _ = h.await;
            }
        }
        "duration" => {
            // 持续时间模式：在 value 秒内不断发请求
            let deadline = start_time + Duration::from_secs(value);
            let semaphore = Arc::new(tokio::sync::Semaphore::new(concurrent as usize));
            let mut handles = Vec::new();

            loop {
                if Instant::now() >= deadline {
                    break;
                }
                let permit = match Arc::clone(&semaphore).try_acquire_owned() {
                    Ok(p) => p,
                    Err(_) => {
                        // 并发槽满，稍等再重试
                        tokio::time::sleep(Duration::from_millis(1)).await;
                        continue;
                    }
                };
                let client_clone = client.clone();
                let params_clone = params.clone();
                let raw_clone = Arc::clone(&raw_stats);

                let h = tokio::spawn(async move {
                    let t = Instant::now();
                    let ok = send(&client_clone, &params_clone).await.is_ok();
                    let dur = t.elapsed().as_millis() as u64;
                    raw_clone.lock().unwrap().record(ok, dur);
                    drop(permit);
                });
                handles.push(h);
            }

            // 等待已派发的请求完成
            for h in handles {
                let _ = h.await;
            }
        }
        _ => {
            return Err(crate::error::AppError::Custom(
                "mode 必须为 'count' 或 'duration'".to_string(),
            ));
        }
    }

    // 通知定时推送任务结束
    let _ = stop_tx.send(());
    // 稍等，确保最终快照 emit 完成
    tokio::time::sleep(Duration::from_millis(300)).await;

    Ok(())
}
```

### Step 2：在 `Cargo.toml` 中确认 libc 依赖（Unix fd 检测需要）

在 `[dependencies]` 末尾追加：
```toml
# Unix fd 上限检测（§10.2）；Windows 编译时此 crate 无 Unix syscall，安全忽略
[target.'cfg(unix)'.dependencies]
libc = "0.2"
```

### Step 3：注册到 `commands/mod.rs`

在 `mod.rs` 追加：
```rust
pub mod stress;
```

### Step 4：注册到 `lib.rs`

在 `use commands::{...}` 块追加：
```rust
    stress::start_stress,
```

在 `invoke_handler![]` 追加：
```rust
            start_stress,
```

### Step 5：cargo check

```powershell
powershell -NonInteractive -Command '
$vsdev = "C:\Program Files\Microsoft Visual Studio\2022\Community\Common7\Tools\VsDevCmd.bat"
$psi = New-Object System.Diagnostics.ProcessStartInfo("cmd.exe")
$psi.Arguments = "/c `"$vsdev`" && cargo check 2>&1"
$psi.UseShellExecute = $false
$psi.RedirectStandardOutput = $true
$psi.RedirectStandardError = $true
$psi.WorkingDirectory = "D:\ProgramFiles\rangeric\apicat\src-tauri"
$p = [System.Diagnostics.Process]::Start($psi)
$out = $p.StandardOutput.ReadToEnd()
$err = $p.StandardError.ReadToEnd()
$p.WaitForExit()
Write-Output $out; Write-Output $err; Write-Output "EXIT: $($p.ExitCode)"
'
# 期望：EXIT: 0
```

### Step 6：提交

```bash
git add src-tauri/src/commands/stress.rs \
        src-tauri/src/commands/mod.rs \
        src-tauri/src/lib.rs \
        src-tauri/Cargo.toml
git commit -m "feat(m6): add stress test engine with tokio concurrency and 200ms aggregated events"
```

---

## Task 3：前端 — stress store + 类型定义

**文件：**
- 修改：`src/types/index.ts`（追加 StressStats、StressConfig 类型）
- 新建：`src/stores/stress.ts`

### Step 1：在 `types/index.ts` 末尾追加

```typescript
// ── 压测类型 ──────────────────────────────────────────────
export interface StressStats {
  total: number
  success: number
  failed: number
  success_rate: number   // 0.0 ~ 100.0
  avg_ms: number
  p50_ms: number
  p95_ms: number
  p99_ms: number
  tps: number
  elapsed_sec: number
  done: boolean
}

export interface StressConfig {
  concurrent: number     // 1 ~ 500
  mode: 'count' | 'duration'
  value: number          // 总请求数 或 持续秒数
}

// 折线图数据点（每次 stress://progress 推送时追加一条）
export interface StressChartPoint {
  time: number           // 相对压测开始的秒数
  tps: number
  avg_ms: number
  p95_ms: number
}
```

### Step 2：新建 `src/stores/stress.ts`

```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { invoke } from '@tauri-apps/api/core'
import { listen, type UnlistenFn } from '@tauri-apps/api/event'
import type { StressStats, StressConfig, StressChartPoint } from '../types'

export const useStressStore = defineStore('stress', () => {
  const isRunning = ref(false)
  const stats = ref<StressStats | null>(null)
  const chartPoints = ref<StressChartPoint[]>([])
  const error = ref<string | null>(null)

  // 取消订阅函数（组件卸载时调用）
  let unlistenProgress: UnlistenFn | null = null
  let unlistenDone: UnlistenFn | null = null

  const successRate = computed(() => stats.value?.success_rate ?? 0)
  const isDone = computed(() => stats.value?.done ?? false)

  /**
   * 启动压测
   * params：SendRequestParams 结构（与普通发请求相同）
   */
  async function startStress(
    params: {
      method: string
      url: string
      query_params: Array<{ key: string; value: string; enabled: boolean }>
      headers: Array<{ key: string; value: string; enabled: boolean }>
      body_type: string
      body: string
      path_params: Array<{ key: string; value: string; enabled: boolean }>
    },
    config: StressConfig
  ) {
    // 重置状态
    stats.value = null
    chartPoints.value = []
    error.value = null
    isRunning.value = true

    // 订阅进度事件
    unlistenProgress = await listen<StressStats>('stress://progress', (event) => {
      stats.value = event.payload
      // 追加折线图数据点
      chartPoints.value.push({
        time: event.payload.elapsed_sec,
        tps: event.payload.tps,
        avg_ms: event.payload.avg_ms,
        p95_ms: event.payload.p95_ms,
      })
    })

    // 订阅完成事件
    unlistenDone = await listen<StressStats>('stress://done', (event) => {
      stats.value = event.payload
      isRunning.value = false
      cleanup()
    })

    try {
      await invoke('start_stress', {
        params,
        concurrent: config.concurrent,
        mode: config.mode,
        value: config.value,
      })
    } catch (e) {
      error.value = String(e)
      isRunning.value = false
      cleanup()
    }
  }

  function cleanup() {
    unlistenProgress?.()
    unlistenDone?.()
    unlistenProgress = null
    unlistenDone = null
  }

  function reset() {
    stats.value = null
    chartPoints.value = []
    error.value = null
    isRunning.value = false
    cleanup()
  }

  return {
    isRunning,
    stats,
    chartPoints,
    error,
    successRate,
    isDone,
    startStress,
    reset,
    cleanup,
  }
})
```

### Step 3：vue-tsc 验证

```bash
npx vue-tsc --noEmit
# 期望：无报错
```

### Step 4：提交

```bash
git add src/types/index.ts src/stores/stress.ts
git commit -m "feat(m6): add StressStats type and stress Pinia store"
```

---

## Task 4：前端 — StressConfigModal.vue

**文件：**
- 新建：`src/components/stress/StressConfigModal.vue`

### Step 1：新建组件

```vue
<template>
  <n-modal
    v-model:show="show"
    preset="dialog"
    title="压测配置"
    :show-icon="false"
    style="width: 420px"
    positive-text="开始压测"
    negative-text="取消"
    @positive-click="handleStart"
    @negative-click="show = false"
  >
    <div class="stress-config">
      <!-- 并发数 -->
      <div class="config-row">
        <span class="config-label">并发数</span>
        <n-input-number
          v-model:value="config.concurrent"
          :min="1"
          :max="500"
          size="small"
          style="width: 120px"
        />
        <span class="config-hint">1 ~ 500</span>
      </div>

      <!-- 模式选择 -->
      <div class="config-row">
        <span class="config-label">模式</span>
        <n-radio-group v-model:value="config.mode" size="small">
          <n-radio value="count">总请求数</n-radio>
          <n-radio value="duration">持续时间</n-radio>
        </n-radio-group>
      </div>

      <!-- 数值 -->
      <div class="config-row">
        <span class="config-label">
          {{ config.mode === 'count' ? '总请求数' : '持续时间（秒）' }}
        </span>
        <n-input-number
          v-model:value="config.value"
          :min="1"
          :max="config.mode === 'count' ? 100000 : 3600"
          size="small"
          style="width: 120px"
        />
        <span class="config-hint">
          {{ config.mode === 'count' ? '条' : '秒' }}
        </span>
      </div>

      <!-- 预估提示 -->
      <div class="config-tip" v-if="config.mode === 'count'">
        💡 {{ config.concurrent }} 并发 × {{ config.value }} 请求
      </div>
      <div class="config-tip" v-else>
        💡 {{ config.concurrent }} 并发持续 {{ config.value }} 秒
      </div>
    </div>
  </n-modal>
</template>

<script setup lang="ts">
import { reactive } from 'vue'
import { NModal, NInputNumber, NRadioGroup, NRadio } from 'naive-ui'
import type { StressConfig } from '../../types'

const show = defineModel<boolean>('show', { required: true })

const emit = defineEmits<{
  start: [config: StressConfig]
}>()

const config = reactive<StressConfig>({
  concurrent: 10,
  mode: 'count',
  value: 100,
})

function handleStart() {
  emit('start', { ...config })
  show.value = false
}
</script>

<style scoped>
.stress-config {
  display: flex;
  flex-direction: column;
  gap: 16px;
  padding: 8px 0;
}

.config-row {
  display: flex;
  align-items: center;
  gap: 12px;
}

.config-label {
  width: 110px;
  font-size: 13px;
  color: var(--n-text-color, #333);
  flex-shrink: 0;
}

.config-hint {
  font-size: 12px;
  color: var(--n-text-color-3, #999);
}

.config-tip {
  font-size: 12px;
  color: var(--n-text-color-3, #999);
  padding: 4px 8px;
  background: var(--n-color-modal, #f9f9f9);
  border-radius: 4px;
}
</style>
```

### Step 2：提交

```bash
git add src/components/stress/StressConfigModal.vue
git commit -m "feat(m6): add StressConfigModal component"
```

---

## Task 5：前端 — StressResultPanel.vue（实时折线图）

**文件：**
- 新建：`src/components/stress/StressResultPanel.vue`

> 使用原生 `<canvas>` 绘制折线图（无需引入额外图表库，减少包体积）。

### Step 1：新建组件

```vue
<template>
  <n-modal
    v-model:show="show"
    preset="card"
    title="压测结果"
    style="width: 680px"
    :closable="!stressStore.isRunning"
    :mask-closable="!stressStore.isRunning"
  >
    <!-- 统计数字卡片 -->
    <div class="stats-grid" v-if="stressStore.stats">
      <div class="stat-card">
        <div class="stat-value">{{ stressStore.stats.total }}</div>
        <div class="stat-label">总请求</div>
      </div>
      <div class="stat-card success">
        <div class="stat-value">{{ stressStore.stats.success }}</div>
        <div class="stat-label">成功</div>
      </div>
      <div class="stat-card fail">
        <div class="stat-value">{{ stressStore.stats.failed }}</div>
        <div class="stat-label">失败</div>
      </div>
      <div class="stat-card">
        <div class="stat-value">{{ stressStore.stats.success_rate.toFixed(1) }}%</div>
        <div class="stat-label">成功率</div>
      </div>
      <div class="stat-card">
        <div class="stat-value">{{ stressStore.stats.tps.toFixed(1) }}</div>
        <div class="stat-label">TPS</div>
      </div>
      <div class="stat-card">
        <div class="stat-value">{{ stressStore.stats.avg_ms.toFixed(0) }}ms</div>
        <div class="stat-label">平均耗时</div>
      </div>
      <div class="stat-card">
        <div class="stat-value">{{ stressStore.stats.p50_ms }}ms</div>
        <div class="stat-label">P50</div>
      </div>
      <div class="stat-card">
        <div class="stat-value">{{ stressStore.stats.p95_ms }}ms</div>
        <div class="stat-label">P95</div>
      </div>
      <div class="stat-card">
        <div class="stat-value">{{ stressStore.stats.p99_ms }}ms</div>
        <div class="stat-label">P99</div>
      </div>
    </div>

    <!-- 实时折线图（canvas）-->
    <div class="chart-area">
      <canvas ref="canvasRef" width="620" height="180" class="stress-canvas" />
      <div class="chart-legend">
        <span class="legend-item tps-color">▬ TPS</span>
        <span class="legend-item avg-color">▬ 平均耗时(ms)</span>
        <span class="legend-item p95-color">▬ P95(ms)</span>
      </div>
    </div>

    <!-- 进行中提示 / 完成提示 -->
    <div class="status-bar" v-if="stressStore.isRunning">
      <n-spin size="small" />
      <span>压测进行中... {{ stressStore.stats?.elapsed_sec.toFixed(1) }}s</span>
    </div>
    <div class="status-bar done" v-else-if="stressStore.stats?.done">
      ✅ 压测完成，耗时 {{ stressStore.stats?.elapsed_sec.toFixed(2) }}s
    </div>
    <div class="status-bar error" v-if="stressStore.error">
      ❌ {{ stressStore.error }}
    </div>

    <template #footer>
      <n-button @click="handleClose" :disabled="stressStore.isRunning">关闭</n-button>
    </template>
  </n-modal>
</template>

<script setup lang="ts">
import { ref, watch, onUnmounted, nextTick } from 'vue'
import { NModal, NButton, NSpin } from 'naive-ui'
import { useStressStore } from '../../stores/stress'

const show = defineModel<boolean>('show', { required: true })
const stressStore = useStressStore()
const canvasRef = ref<HTMLCanvasElement | null>(null)

// ── Canvas 折线图绘制 ────────────────────────────────────────

function drawChart() {
  const canvas = canvasRef.value
  if (!canvas) return
  const ctx = canvas.getContext('2d')
  if (!ctx) return

  const points = stressStore.chartPoints
  const W = canvas.width
  const H = canvas.height
  const PAD = { top: 16, right: 16, bottom: 24, left: 48 }
  const innerW = W - PAD.left - PAD.right
  const innerH = H - PAD.top - PAD.bottom

  ctx.clearRect(0, 0, W, H)

  // 背景
  ctx.fillStyle = '#fafafa'
  ctx.fillRect(0, 0, W, H)

  if (points.length < 2) {
    ctx.fillStyle = '#999'
    ctx.font = '12px sans-serif'
    ctx.textAlign = 'center'
    ctx.fillText('等待数据...', W / 2, H / 2)
    return
  }

  // 计算 Y 轴最大值（TPS 和 ms 都画在同一 Y 轴，用归一化）
  const maxTps = Math.max(...points.map(p => p.tps), 1)
  const maxMs = Math.max(...points.map(p => p.p95_ms), 1)

  // 网格线
  ctx.strokeStyle = '#e8e8e8'
  ctx.lineWidth = 1
  for (let i = 0; i <= 4; i++) {
    const y = PAD.top + (innerH * i) / 4
    ctx.beginPath()
    ctx.moveTo(PAD.left, y)
    ctx.lineTo(PAD.left + innerW, y)
    ctx.stroke()
  }

  const xForIdx = (i: number) => PAD.left + (i / (points.length - 1)) * innerW

  // 绘制折线函数
  function drawLine(
    values: number[],
    maxVal: number,
    color: string,
    lineWidth = 1.5
  ) {
    ctx!.strokeStyle = color
    ctx!.lineWidth = lineWidth
    ctx!.lineJoin = 'round'
    ctx!.beginPath()
    values.forEach((v, i) => {
      const x = xForIdx(i)
      const y = PAD.top + innerH - (v / maxVal) * innerH
      if (i === 0) ctx!.moveTo(x, y)
      else ctx!.lineTo(x, y)
    })
    ctx!.stroke()
  }

  drawLine(points.map(p => p.tps), maxTps, '#18a058', 2)      // TPS — 绿色
  drawLine(points.map(p => p.avg_ms), maxMs, '#2080f0', 1.5)  // avg — 蓝色
  drawLine(points.map(p => p.p95_ms), maxMs, '#f0a020', 1.5)  // p95 — 橙色

  // X 轴时间标签
  ctx.fillStyle = '#999'
  ctx.font = '10px sans-serif'
  ctx.textAlign = 'center'
  const labelCount = Math.min(5, points.length)
  for (let i = 0; i < labelCount; i++) {
    const idx = Math.floor((i / (labelCount - 1)) * (points.length - 1))
    const x = xForIdx(idx)
    ctx.fillText(`${points[idx].time.toFixed(0)}s`, x, H - 4)
  }
}

// 监听 chartPoints 变化，重新绘制
watch(
  () => stressStore.chartPoints.length,
  () => nextTick(drawChart)
)

// 弹窗打开时初始化 canvas
watch(show, (v) => {
  if (v) nextTick(drawChart)
})

function handleClose() {
  stressStore.reset()
  show.value = false
}

onUnmounted(() => {
  stressStore.cleanup()
})
</script>

<style scoped>
.stats-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 8px;
  margin-bottom: 16px;
}

.stat-card {
  background: var(--n-color-modal, #f6f6f6);
  border-radius: 6px;
  padding: 10px 12px;
  text-align: center;
}
.stat-card.success .stat-value { color: #18a058; }
.stat-card.fail .stat-value { color: #d03050; }

.stat-value {
  font-size: 20px;
  font-weight: 600;
  line-height: 1.2;
}
.stat-label {
  font-size: 11px;
  color: var(--n-text-color-3, #999);
  margin-top: 2px;
}

.chart-area {
  border: 1px solid var(--n-border-color, #e0e0e6);
  border-radius: 6px;
  overflow: hidden;
  margin-bottom: 12px;
}

.stress-canvas {
  display: block;
  width: 100%;
  height: 180px;
}

.chart-legend {
  display: flex;
  gap: 16px;
  padding: 6px 12px;
  font-size: 11px;
  background: var(--n-color-modal, #f6f6f6);
  border-top: 1px solid var(--n-border-color, #e0e0e6);
}
.tps-color { color: #18a058; }
.avg-color { color: #2080f0; }
.p95-color { color: #f0a020; }

.status-bar {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 13px;
  color: var(--n-text-color-3, #999);
  padding: 4px 0;
}
.status-bar.done { color: #18a058; }
.status-bar.error { color: #d03050; }
</style>
```

### Step 2：提交

```bash
git add src/components/stress/StressResultPanel.vue
git commit -m "feat(m6): add StressResultPanel with canvas realtime chart"
```

---

## Task 6：MainPanel.vue 集成压测按钮 + 弹窗

**文件：**
- 修改：`src/components/layout/MainPanel.vue`

### Step 1：在 script 区引入压测相关组件和 store

在 import 区追加：
```typescript
import StressConfigModal from '../stress/StressConfigModal.vue'
import StressResultPanel from '../stress/StressResultPanel.vue'
import { useStressStore } from '../../stores/stress'
import type { StressConfig } from '../../types'

const stressStore = useStressStore()
const showStressConfig = ref(false)
const showStressResult = ref(false)
```

### Step 2：新增 handleStartStress 函数

在 script 区追加：
```typescript
async function handleStartStress(config: StressConfig) {
  // 构建与 handleSend 相同的请求参数（复用当前编辑区内容）
  // 先同步 kv/json 模式内容
  if (queryMode.value === 'kv') queryParams.value = parseKvText(queryKvText.value)
  else if (queryMode.value === 'json') queryParams.value = parseJsonToParams(queryJsonText.value)
  if (headerMode.value === 'kv') requestHeaders.value = parseKvText(headerKvText.value)
  else if (headerMode.value === 'json') requestHeaders.value = parseJsonToParams(headerJsonText.value)

  const req = requestStore.activeRequest
  const activeEnv = envStore.activeEnv

  // 变量替换（URL 和 body 中的 {{var}}）
  let resolvedUrlForStress = url.value
  if (activeEnv?.variables) {
    for (const v of activeEnv.variables) {
      if (v.enabled) {
        resolvedUrlForStress = resolvedUrlForStress.replaceAll(`{{${v.key}}}`, v.value)
      }
    }
  }

  showStressResult.value = true

  await stressStore.startStress(
    {
      method: method.value,
      url: resolvedUrlForStress,
      query_params: queryParams.value.map(p => ({ key: p.key, value: p.value, enabled: p.enabled })),
      headers: requestHeaders.value.map(h => ({ key: h.key, value: h.value, enabled: h.enabled })),
      body_type: bodyType.value,
      body: bodyContent.value,
      path_params: [],
    },
    config
  )
}
```

### Step 3：在 URL 栏旁添加「压测」按钮

找到 URL 栏发送按钮：
```html
            <n-button type="primary" :loading="isSending" @click="handleSend">发送</n-button>
```

在其后（`</div>` 之前）追加：
```html
            <n-button
              type="default"
              title="压测"
              :disabled="!url || isSending"
              @click="showStressConfig = true"
            >⚡ 压测</n-button>
```

### Step 4：在模板末尾（`</template>` 之前）添加弹窗

```html
  <!-- 压测配置弹窗 -->
  <StressConfigModal
    v-model:show="showStressConfig"
    @start="handleStartStress"
  />

  <!-- 压测结果弹窗 -->
  <StressResultPanel v-model:show="showStressResult" />
```

### Step 5：vue-tsc 验证

```bash
npx vue-tsc --noEmit
# 期望：无报错
```

### Step 6：提交

```bash
git add src/components/layout/MainPanel.vue
git commit -m "feat(m6): integrate stress test button and modals into MainPanel"
```

---

## Task 7：Rust — 导出（ApiCat 格式 + Postman v2.1）

**文件：**
- 新建：`src-tauri/src/commands/io.rs`（仅导出部分）
- 修改：`src-tauri/src/commands/mod.rs`
- 修改：`src-tauri/src/lib.rs`

### Step 1：在 `types.rs` 末尾追加导出用的序列化结构

```rust
// ── 导入导出用结构 ──────────────────────────────────────────

/// ApiCat 自定义导出格式（完整项目快照）
#[derive(Debug, Serialize, Deserialize)]
pub struct ApiCatExport {
    pub version: String,                 // 格式版本，当前 "1.0"
    pub exported_at: String,             // ISO 8601 时间
    pub project: ExportProject,
}

#[derive(Debug, Serialize, Deserialize)]
pub struct ExportProject {
    pub name: String,
    pub description: Option<String>,
    pub environments: Vec<ExportEnv>,
    pub collections: Vec<ExportCollection>,
}

#[derive(Debug, Serialize, Deserialize)]
pub struct ExportEnv {
    pub name: String,
    pub base_url: Option<String>,
    pub is_active: i64,
    pub variables: Vec<ExportEnvVar>,
}

#[derive(Debug, Serialize, Deserialize)]
pub struct ExportEnvVar {
    pub key: String,
    pub value: String,
    pub description: Option<String>,
    pub enabled: i64,
}

#[derive(Debug, Serialize, Deserialize)]
pub struct ExportCollection {
    pub name: String,
    pub sort_order: i64,
    pub children: Vec<ExportCollection>,    // 递归子文件夹
    pub requests: Vec<ExportRequest>,
}

#[derive(Debug, Serialize, Deserialize)]
pub struct ExportRequest {
    pub name: String,
    pub method: String,
    pub url: String,
    pub params: String,         // JSON 数组
    pub headers: String,        // JSON 数组
    pub body_type: String,
    pub body: String,
    pub auth_type: String,
    pub auth_config: String,    // JSON
    pub sort_order: i64,
}
```

### Step 2：新建 `src-tauri/src/commands/io.rs`（导出部分）

```rust
//! 导入导出命令

use crate::{db::AppDb, error::CmdResult, types::*};
use serde::{Deserialize, Serialize};
use tauri::State;

// ═══════════════════════════════════════════════════════════
//  导出
// ═══════════════════════════════════════════════════════════

/// 导出项目为 ApiCat 自定义 JSON 格式（字符串，前端负责写文件）
#[tauri::command]
pub async fn export_apicat(db: State<'_, AppDb>, project_id: i64) -> CmdResult<String> {
    let project = sqlx::query!("SELECT name, description FROM projects WHERE id=?", project_id)
        .fetch_one(&db.0)
        .await
        .map_err(|_| crate::error::AppError::Custom("项目不存在".to_string()))?;

    // 导出环境变量
    let envs = export_environments(&db, project_id).await?;

    // 导出接口树（递归）
    let collections = export_collections_tree(&db, project_id, None).await?;

    let export = ApiCatExport {
        version: "1.0".to_string(),
        exported_at: chrono::Utc::now().to_rfc3339(),
        project: ExportProject {
            name: project.name,
            description: project.description,
            environments: envs,
            collections,
        },
    };

    Ok(serde_json::to_string_pretty(&export)
        .map_err(|e| crate::error::AppError::Custom(format!("序列化失败: {e}")))?)
}

async fn export_environments(db: &AppDb, project_id: i64) -> CmdResult<Vec<ExportEnv>> {
    let envs = sqlx::query!(
        "SELECT id, name, base_url, is_active FROM environments WHERE project_id=?",
        project_id
    )
    .fetch_all(&db.0)
    .await?;

    let mut result = Vec::new();
    for env in envs {
        let vars = sqlx::query!(
            "SELECT key, value, description, enabled FROM env_variables WHERE env_id=?",
            env.id
        )
        .fetch_all(&db.0)
        .await?;

        result.push(ExportEnv {
            name: env.name,
            base_url: env.base_url,
            is_active: env.is_active,
            variables: vars
                .into_iter()
                .map(|v| ExportEnvVar {
                    key: v.key,
                    value: v.value,
                    description: v.description,
                    enabled: v.enabled,
                })
                .collect(),
        });
    }
    Ok(result)
}

/// 递归导出接口树
async fn export_collections_tree(
    db: &AppDb,
    project_id: i64,
    parent_id: Option<i64>,
) -> CmdResult<Vec<ExportCollection>> {
    // 查当前层 collections
    let colls = match parent_id {
        Some(pid) => sqlx::query!(
            "SELECT id, name, sort_order FROM collections WHERE project_id=? AND parent_id=? ORDER BY sort_order, id",
            project_id, pid
        ).fetch_all(&db.0).await?,
        None => sqlx::query!(
            "SELECT id, name, sort_order FROM collections WHERE project_id=? AND parent_id IS NULL ORDER BY sort_order, id",
            project_id
        ).fetch_all(&db.0).await?,
    };

    let mut result = Vec::new();
    for coll in colls {
        // 查该 collection 下的接口
        let reqs = sqlx::query!(
            "SELECT name, method, url, params, headers, body_type, body, auth_type, auth_config, sort_order \
             FROM api_requests WHERE collection_id=? ORDER BY sort_order, id",
            coll.id
        )
        .fetch_all(&db.0)
        .await?;

        // 递归查子 collections
        let children = Box::pin(export_collections_tree(db, project_id, Some(coll.id))).await?;

        result.push(ExportCollection {
            name: coll.name,
            sort_order: coll.sort_order,
            children,
            requests: reqs
                .into_iter()
                .map(|r| ExportRequest {
                    name: r.name,
                    method: r.method,
                    url: r.url,
                    params: r.params.unwrap_or_default(),
                    headers: r.headers.unwrap_or_default(),
                    body_type: r.body_type.unwrap_or_default(),
                    body: r.body.unwrap_or_default(),
                    auth_type: r.auth_type.unwrap_or_default(),
                    auth_config: r.auth_config.unwrap_or_default(),
                    sort_order: r.sort_order,
                })
                .collect(),
        });
    }
    Ok(result)
}

/// 导出为 Postman Collection v2.1 JSON 字符串
#[tauri::command]
pub async fn export_postman(db: State<'_, AppDb>, project_id: i64) -> CmdResult<String> {
    let project = sqlx::query!("SELECT name FROM projects WHERE id=?", project_id)
        .fetch_one(&db.0)
        .await
        .map_err(|_| crate::error::AppError::Custom("项目不存在".to_string()))?;

    let collections = export_collections_tree(&db, project_id, None).await?;

    // 构建 Postman v2.1 结构
    let postman = build_postman_collection(&project.name, &collections);

    Ok(serde_json::to_string_pretty(&postman)
        .map_err(|e| crate::error::AppError::Custom(format!("序列化失败: {e}")))?)
}

/// 将 ExportCollection[] 转换为 Postman v2.1 JSON Value
fn build_postman_collection(name: &str, collections: &[ExportCollection]) -> serde_json::Value {
    fn build_items(colls: &[ExportCollection]) -> Vec<serde_json::Value> {
        let mut items = Vec::new();
        for coll in colls {
            let mut folder_items: Vec<serde_json::Value> = coll
                .requests
                .iter()
                .map(|r| {
                    let url_obj = serde_json::json!({
                        "raw": r.url,
                        "host": [r.url.split('/').nth(2).unwrap_or("")],
                        "path": r.url.split('/').skip(3).collect::<Vec<_>>()
                    });

                    // 解析 headers
                    let headers: Vec<serde_json::Value> = serde_json::from_str::<Vec<serde_json::Value>>(&r.headers)
                        .unwrap_or_default()
                        .into_iter()
                        .filter(|h| h.get("enabled").and_then(|e| e.as_bool()).unwrap_or(true))
                        .map(|h| serde_json::json!({
                            "key": h["key"],
                            "value": h["value"],
                        }))
                        .collect();

                    let body = if r.body_type == "raw_json" {
                        serde_json::json!({
                            "mode": "raw",
                            "raw": r.body,
                            "options": { "raw": { "language": "json" } }
                        })
                    } else if r.body_type == "form_urlencoded" {
                        serde_json::json!({ "mode": "urlencoded", "urlencoded": [] })
                    } else {
                        serde_json::json!({ "mode": "raw", "raw": "" })
                    };

                    serde_json::json!({
                        "name": r.name,
                        "request": {
                            "method": r.method,
                            "header": headers,
                            "url": url_obj,
                            "body": body,
                        },
                        "response": []
                    })
                })
                .collect();

            folder_items.extend(build_items(&coll.children));

            items.push(serde_json::json!({
                "name": coll.name,
                "item": folder_items,
            }));
        }
        items
    }

    serde_json::json!({
        "info": {
            "name": name,
            "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json",
            "_postman_id": uuid::Uuid::new_v4().to_string(),
        },
        "item": build_items(collections),
    })
}
```

> ⚠️ `uuid` crate 用于生成 Postman collection ID，需在 Cargo.toml 中添加：
> ```toml
> uuid = { version = "1", features = ["v4"] }
> ```
> `chrono` crate 用于导出时间戳，也需添加：
> ```toml
> chrono = { version = "0.4", features = ["serde"] }
> ```

### Step 3：更新 Cargo.toml

在 `[dependencies]` 末尾追加：
```toml
uuid = { version = "1", features = ["v4"] }
chrono = { version = "0.4", features = ["serde"] }
```

### Step 4：注册到 `commands/mod.rs`

追加：
```rust
pub mod io;
```

### Step 5：注册到 `lib.rs`

在 `use commands::{...}` 块追加：
```rust
    io::{export_apicat, export_postman, import_apicat, import_postman, import_openapi},
```

> ⚠️ import_* 函数在 Task 8 中实现，这里先只注册 export_*，Task 8 完成后再补 import_*。
> 所以 Task 7 时先写：
> ```rust
>     io::{export_apicat, export_postman},
> ```

在 `invoke_handler![]` 追加：
```rust
            export_apicat, export_postman,
```

### Step 6：cargo check

```powershell
# 同上 cargo check 命令
# 期望：EXIT: 0
```

### Step 7：提交

```bash
git add src-tauri/src/commands/io.rs \
        src-tauri/src/commands/mod.rs \
        src-tauri/src/types.rs \
        src-tauri/src/lib.rs \
        src-tauri/Cargo.toml
git commit -m "feat(m6): add ApiCat and Postman v2.1 export commands"
```

---

## Task 8：Rust — 导入（Postman v2.1 + OpenAPI 3.x）

**文件：**
- 修改：`src-tauri/src/commands/io.rs`（追加导入函数）
- 修改：`src-tauri/src/lib.rs`（补充注册 import_* 命令）

### Step 1：在 `io.rs` 末尾追加导入函数

```rust
// ═══════════════════════════════════════════════════════════
//  导入
// ═══════════════════════════════════════════════════════════

/// 导入 ApiCat 自定义格式（JSON 字符串），写入指定项目
/// 若 project_id = 0，则新建项目
#[tauri::command]
pub async fn import_apicat(
    db: State<'_, AppDb>,
    project_id: i64,
    json_content: String,
) -> CmdResult<i64> {
    let export: ApiCatExport = serde_json::from_str(&json_content)
        .map_err(|e| crate::error::AppError::Custom(format!("解析失败: {e}")))?;

    let pid = ensure_project(&db, project_id, &export.project.name, export.project.description.as_deref()).await?;

    // 导入环境变量
    for env in &export.project.environments {
        let env_id: i64 = sqlx::query_scalar!(
            "INSERT INTO environments (project_id, name, base_url, is_active) VALUES (?,?,?,?) RETURNING id",
            pid, env.name, env.base_url, env.is_active
        )
        .fetch_one(&db.0)
        .await?;

        for var in &env.variables {
            sqlx::query!(
                "INSERT INTO env_variables (env_id, key, value, description, enabled) VALUES (?,?,?,?,?)",
                env_id, var.key, var.value, var.description, var.enabled
            )
            .execute(&db.0)
            .await?;
        }
    }

    // 导入接口树（递归）
    import_collections_tree(&db, pid, None, &export.project.collections).await?;

    Ok(pid)
}

/// 导入 Postman Collection v2.1 JSON，写入指定项目
#[tauri::command]
pub async fn import_postman(
    db: State<'_, AppDb>,
    project_id: i64,
    json_content: String,
) -> CmdResult<i64> {
    let v: serde_json::Value = serde_json::from_str(&json_content)
        .map_err(|e| crate::error::AppError::Custom(format!("解析失败: {e}")))?;

    let coll_name = v["info"]["name"].as_str().unwrap_or("导入的接口集合").to_string();
    let pid = ensure_project(&db, project_id, &coll_name, None).await?;

    // 递归导入 item 树
    import_postman_items(&db, pid, None, v["item"].as_array().unwrap_or(&vec![]), 0).await?;

    Ok(pid)
}

/// 递归处理 Postman items
#[async_recursion::async_recursion]
async fn import_postman_items(
    db: &AppDb,
    project_id: i64,
    parent_collection_id: Option<i64>,
    items: &[serde_json::Value],
    sort_order: i64,
) -> CmdResult<()> {
    for (idx, item) in items.iter().enumerate() {
        let name = item["name"].as_str().unwrap_or("未命名").to_string();

        if item.get("item").is_some() {
            // 这是一个文件夹（Folder）
            let coll_id: i64 = sqlx::query_scalar!(
                "INSERT INTO collections (project_id, parent_id, name, sort_order) VALUES (?,?,?,?) RETURNING id",
                project_id, parent_collection_id, name, sort_order + idx as i64
            )
            .fetch_one(&db.0)
            .await?;

            import_postman_items(
                db,
                project_id,
                Some(coll_id),
                item["item"].as_array().unwrap_or(&vec![]),
                0,
            )
            .await?;
        } else {
            // 这是一个请求（Request）
            let req = &item["request"];
            let method = req["method"].as_str().unwrap_or("GET").to_string();
            let url = extract_postman_url(&req["url"]);

            // 提取 headers
            let headers: Vec<serde_json::Value> = req["header"]
                .as_array()
                .unwrap_or(&vec![])
                .iter()
                .map(|h| serde_json::json!({
                    "key": h["key"].as_str().unwrap_or(""),
                    "value": h["value"].as_str().unwrap_or(""),
                    "enabled": true
                }))
                .collect();

            // 提取 body
            let (body_type, body) = extract_postman_body(&req["body"]);

            // 需要一个有效的 collection_id；若 parent 为 None，先创建一个根 collection
            let coll_id = match parent_collection_id {
                Some(id) => id,
                None => {
                    // 单层请求（无文件夹），放到以 collection 名命名的默认文件夹
                    sqlx::query_scalar!(
                        "INSERT OR IGNORE INTO collections (project_id, parent_id, name, sort_order) VALUES (?,NULL,'导入',0) RETURNING id",
                        project_id
                    )
                    .fetch_one(&db.0)
                    .await?
                }
            };

            sqlx::query!(
                "INSERT INTO api_requests (collection_id, name, method, url, headers, body_type, body, sort_order) \
                 VALUES (?,?,?,?,?,?,?,?)",
                coll_id, name, method, url,
                serde_json::to_string(&headers).unwrap_or_default(),
                body_type, body,
                idx as i64
            )
            .execute(&db.0)
            .await?;
        }
    }
    Ok(())
}

fn extract_postman_url(url_val: &serde_json::Value) -> String {
    // url 可能是字符串或对象
    if let Some(s) = url_val.as_str() {
        return s.to_string();
    }
    url_val["raw"].as_str().unwrap_or("").to_string()
}

fn extract_postman_body(body_val: &serde_json::Value) -> (String, String) {
    let mode = body_val["mode"].as_str().unwrap_or("none");
    match mode {
        "raw" => {
            let lang = body_val["options"]["raw"]["language"].as_str().unwrap_or("text");
            let body_type = if lang == "json" { "raw_json" } else { "raw_text" };
            (body_type.to_string(), body_val["raw"].as_str().unwrap_or("").to_string())
        }
        "urlencoded" => ("form_urlencoded".to_string(), "".to_string()),
        _ => ("none".to_string(), "".to_string()),
    }
}

/// 导入 OpenAPI 3.x（JSON 或 YAML 字符串）
#[tauri::command]
pub async fn import_openapi(
    db: State<'_, AppDb>,
    project_id: i64,
    content: String,
    is_yaml: bool,
) -> CmdResult<i64> {
    // 解析为 JSON Value（YAML 先转 JSON）
    let v: serde_json::Value = if is_yaml {
        let yaml_val: serde_yaml::Value = serde_yaml::from_str(&content)
            .map_err(|e| crate::error::AppError::Custom(format!("YAML 解析失败: {e}")))?;
        serde_json::to_value(yaml_val)
            .map_err(|e| crate::error::AppError::Custom(format!("YAML→JSON 转换失败: {e}")))?
    } else {
        serde_json::from_str(&content)
            .map_err(|e| crate::error::AppError::Custom(format!("JSON 解析失败: {e}")))?
    };

    let api_title = v["info"]["title"].as_str().unwrap_or("OpenAPI 导入").to_string();
    let pid = ensure_project(&db, project_id, &api_title, None).await?;

    // 解析 servers[0].url 作为 base_url（可选）
    let base_url = v["servers"][0]["url"].as_str().map(|s| s.to_string());

    // 为每个 tag 创建一个 Collection
    let tags: Vec<String> = v["tags"]
        .as_array()
        .unwrap_or(&vec![])
        .iter()
        .map(|t| t["name"].as_str().unwrap_or("默认").to_string())
        .collect();

    // tag → collection_id 映射
    let mut tag_map: std::collections::HashMap<String, i64> = Default::default();
    for (idx, tag) in tags.iter().enumerate() {
        let coll_id: i64 = sqlx::query_scalar!(
            "INSERT INTO collections (project_id, parent_id, name, sort_order) VALUES (?,NULL,?,?) RETURNING id",
            pid, tag, idx as i64
        )
        .fetch_one(&db.0)
        .await?;
        tag_map.insert(tag.clone(), coll_id);
    }

    // 默认 collection（无 tag 的接口放这里）
    let default_coll_id: i64 = sqlx::query_scalar!(
        "INSERT INTO collections (project_id, parent_id, name, sort_order) VALUES (?,NULL,'其他',999) RETURNING id",
        pid
    )
    .fetch_one(&db.0)
    .await?;

    // 遍历 paths
    let paths = v["paths"].as_object().cloned().unwrap_or_default();
    let mut sort_order = 0i64;

    for (path, path_item) in &paths {
        let methods = ["get", "post", "put", "delete", "patch", "head", "options"];
        for method_str in &methods {
            let op = &path_item[method_str];
            if op.is_null() {
                continue;
            }

            let op_summary = op["summary"].as_str()
                .or_else(|| op["operationId"].as_str())
                .unwrap_or(path.as_str());
            let name = format!("{} {}", method_str.to_uppercase(), op_summary);

            // 确定 collection_id
            let first_tag = op["tags"][0].as_str().unwrap_or("");
            let coll_id = tag_map.get(first_tag).copied().unwrap_or(default_coll_id);

            // 提取 parameters → headers + params
            let parameters = op["parameters"].as_array().cloned().unwrap_or_default();
            let mut headers_json: Vec<serde_json::Value> = Vec::new();
            let mut params_json: Vec<serde_json::Value> = Vec::new();

            for param in &parameters {
                let pname = param["name"].as_str().unwrap_or("").to_string();
                let pvalue = param["schema"]["default"].as_str().unwrap_or("").to_string();
                let enabled = true;
                match param["in"].as_str().unwrap_or("") {
                    "header" => headers_json.push(serde_json::json!({
                        "key": pname, "value": pvalue, "enabled": enabled
                    })),
                    "query" => params_json.push(serde_json::json!({
                        "key": pname, "value": pvalue, "enabled": enabled
                    })),
                    _ => {}
                }
            }

            // 提取 requestBody
            let (body_type, body) = {
                let content = &op["requestBody"]["content"];
                if !content["application/json"].is_null() {
                    let schema = &content["application/json"]["schema"];
                    let example = serde_json::json!({});
                    ("raw_json".to_string(), serde_json::to_string_pretty(schema).unwrap_or_default())
                } else {
                    ("none".to_string(), "".to_string())
                }
            };

            // 构建完整 URL
            let full_url = match &base_url {
                Some(base) => format!("{}{}", base.trim_end_matches('/'), path),
                None => path.clone(),
            };

            sqlx::query!(
                "INSERT INTO api_requests (collection_id, name, method, url, headers, params, body_type, body, sort_order) \
                 VALUES (?,?,?,?,?,?,?,?,?)",
                coll_id, name,
                method_str.to_uppercase(),
                full_url,
                serde_json::to_string(&headers_json).unwrap_or_default(),
                serde_json::to_string(&params_json).unwrap_or_default(),
                body_type, body,
                sort_order
            )
            .execute(&db.0)
            .await?;

            sort_order += 1;
        }
    }

    Ok(pid)
}

/// 辅助函数：若 project_id > 0 则使用现有项目，否则新建项目
async fn ensure_project(
    db: &AppDb,
    project_id: i64,
    name: &str,
    description: Option<&str>,
) -> CmdResult<i64> {
    if project_id > 0 {
        return Ok(project_id);
    }
    let id: i64 = sqlx::query_scalar!(
        "INSERT INTO projects (name, description) VALUES (?,?) RETURNING id",
        name, description
    )
    .fetch_one(&db.0)
    .await?;
    Ok(id)
}
```

### Step 2：在 Cargo.toml 中添加 async-recursion

```toml
async-recursion = "1"
```

### Step 3：在 `lib.rs` 更新 import 注册

将 Task 7 中的：
```rust
    io::{export_apicat, export_postman},
```
替换为：
```rust
    io::{export_apicat, export_postman, import_apicat, import_postman, import_openapi},
```

将 `invoke_handler![]` 中的 export 行更新为：
```rust
            export_apicat, export_postman, import_apicat, import_postman, import_openapi,
```

### Step 4：cargo check

```powershell
# 同上 cargo check 命令
# 期望：EXIT: 0
```

### Step 5：提交

```bash
git add src-tauri/src/commands/io.rs src-tauri/src/lib.rs src-tauri/Cargo.toml
git commit -m "feat(m6): add Postman v2.1 and OpenAPI 3.x import commands"
```

---

## Task 9：前端 — ImportDialog.vue + ExportDialog.vue

**文件：**
- 新建：`src/components/io/ImportDialog.vue`
- 新建：`src/components/io/ExportDialog.vue`

### Step 1：新建 `src/components/io/ImportDialog.vue`

```vue
<template>
  <n-modal
    v-model:show="show"
    preset="dialog"
    title="导入接口"
    :show-icon="false"
    style="width: 460px"
    positive-text="导入"
    negative-text="取消"
    :loading="loading"
    @positive-click="handleImport"
    @negative-click="show = false"
  >
    <div class="import-body">
      <!-- 导入格式选择 -->
      <div class="field-row">
        <span class="field-label">导入格式</span>
        <n-radio-group v-model:value="format" size="small">
          <n-radio value="postman">Postman Collection v2.1</n-radio>
          <n-radio value="openapi">OpenAPI 3.x（JSON / YAML）</n-radio>
          <n-radio value="apicat">ApiCat 格式</n-radio>
        </n-radio-group>
      </div>

      <!-- 目标项目 -->
      <div class="field-row">
        <span class="field-label">导入到</span>
        <n-select
          v-model:value="targetProjectId"
          :options="projectOptions"
          size="small"
          style="flex: 1"
          placeholder="新建项目"
        />
      </div>

      <!-- 文件选择 -->
      <div class="field-row">
        <span class="field-label">文件</span>
        <n-button size="small" @click="selectFile">📂 选择文件</n-button>
        <span class="file-name" v-if="selectedFileName">{{ selectedFileName }}</span>
      </div>

      <!-- 错误提示 -->
      <div class="import-error" v-if="errorMsg">❌ {{ errorMsg }}</div>

      <!-- 成功提示 -->
      <div class="import-success" v-if="successMsg">✅ {{ successMsg }}</div>
    </div>
  </n-modal>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import { NModal, NRadioGroup, NRadio, NSelect, NButton } from 'naive-ui'
import { invoke } from '@tauri-apps/api/core'
import { open } from '@tauri-apps/plugin-dialog'
import { readTextFile } from '@tauri-apps/plugin-fs'
import { useProjectStore } from '../../stores/project'

const show = defineModel<boolean>('show', { required: true })
const emit = defineEmits<{ imported: [projectId: number] }>()

const projectStore = useProjectStore()

const format = ref<'postman' | 'openapi' | 'apicat'>('postman')
const targetProjectId = ref<number | null>(null)
const selectedFilePath = ref<string | null>(null)
const selectedFileName = ref('')
const loading = ref(false)
const errorMsg = ref('')
const successMsg = ref('')

const projectOptions = computed(() => [
  { label: '新建项目', value: null },
  ...projectStore.projects.map(p => ({ label: p.name, value: p.id })),
])

async function selectFile() {
  errorMsg.value = ''
  const filters = format.value === 'openapi'
    ? [{ name: 'OpenAPI', extensions: ['json', 'yaml', 'yml'] }]
    : [{ name: 'JSON', extensions: ['json'] }]

  const selected = await open({ filters, multiple: false })
  if (!selected || typeof selected !== 'string') return
  selectedFilePath.value = selected
  selectedFileName.value = selected.split(/[/\\]/).pop() ?? selected
}

async function handleImport() {
  if (!selectedFilePath.value) {
    errorMsg.value = '请先选择文件'
    return false  // 阻止 modal 关闭
  }

  loading.value = true
  errorMsg.value = ''
  successMsg.value = ''

  try {
    const content = await readTextFile(selectedFilePath.value)
    const pid = targetProjectId.value ?? 0

    let importedPid: number
    if (format.value === 'postman') {
      importedPid = await invoke<number>('import_postman', { project_id: pid, json_content: content })
    } else if (format.value === 'apicat') {
      importedPid = await invoke<number>('import_apicat', { project_id: pid, json_content: content })
    } else {
      const isYaml = selectedFilePath.value.endsWith('.yaml') || selectedFilePath.value.endsWith('.yml')
      importedPid = await invoke<number>('import_openapi', { project_id: pid, content, is_yaml: isYaml })
    }

    await projectStore.loadProjects()
    successMsg.value = `导入成功，项目 ID: ${importedPid}`
    emit('imported', importedPid)

    // 2秒后自动关闭
    setTimeout(() => { show.value = false }, 2000)
  } catch (e) {
    errorMsg.value = String(e)
  } finally {
    loading.value = false
  }

  return false  // 防止 modal 自动关闭（等待成功提示展示）
}
</script>

<style scoped>
.import-body {
  display: flex;
  flex-direction: column;
  gap: 14px;
  padding: 4px 0;
}
.field-row {
  display: flex;
  align-items: flex-start;
  gap: 12px;
}
.field-label {
  width: 70px;
  font-size: 13px;
  flex-shrink: 0;
  padding-top: 3px;
}
.file-name {
  font-size: 12px;
  color: var(--n-text-color-3, #999);
  word-break: break-all;
}
.import-error { font-size: 12px; color: #d03050; }
.import-success { font-size: 12px; color: #18a058; }
</style>
```

### Step 2：新建 `src/components/io/ExportDialog.vue`

```vue
<template>
  <n-modal
    v-model:show="show"
    preset="dialog"
    title="导出接口"
    :show-icon="false"
    style="width: 400px"
    positive-text="导出"
    negative-text="取消"
    :loading="loading"
    @positive-click="handleExport"
    @negative-click="show = false"
  >
    <div class="export-body">
      <!-- 导出格式 -->
      <div class="field-row">
        <span class="field-label">导出格式</span>
        <n-radio-group v-model:value="format" size="small">
          <n-radio value="apicat">ApiCat 格式（含环境变量）</n-radio>
          <n-radio value="postman">Postman Collection v2.1</n-radio>
        </n-radio-group>
      </div>

      <!-- 导出项目 -->
      <div class="field-row">
        <span class="field-label">导出项目</span>
        <n-select
          v-model:value="exportProjectId"
          :options="projectOptions"
          size="small"
          style="flex: 1"
        />
      </div>

      <!-- 错误 / 成功 -->
      <div class="export-error" v-if="errorMsg">❌ {{ errorMsg }}</div>
      <div class="export-success" v-if="successMsg">✅ {{ successMsg }}</div>
    </div>
  </n-modal>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import { NModal, NRadioGroup, NRadio, NSelect } from 'naive-ui'
import { invoke } from '@tauri-apps/api/core'
import { save } from '@tauri-apps/plugin-dialog'
import { writeTextFile } from '@tauri-apps/plugin-fs'
import { useProjectStore } from '../../stores/project'

const show = defineModel<boolean>('show', { required: true })

const projectStore = useProjectStore()

const format = ref<'apicat' | 'postman'>('apicat')
const exportProjectId = ref<number | null>(
  projectStore.currentProjectId ?? null
)
const loading = ref(false)
const errorMsg = ref('')
const successMsg = ref('')

const projectOptions = computed(() =>
  projectStore.projects.map(p => ({ label: p.name, value: p.id }))
)

async function handleExport() {
  if (!exportProjectId.value) {
    errorMsg.value = '请选择要导出的项目'
    return false
  }

  loading.value = true
  errorMsg.value = ''
  successMsg.value = ''

  try {
    // 调用 Rust 命令获取导出内容
    let content: string
    let defaultFilename: string

    if (format.value === 'apicat') {
      content = await invoke<string>('export_apicat', { project_id: exportProjectId.value })
      defaultFilename = 'apicat-export.json'
    } else {
      content = await invoke<string>('export_postman', { project_id: exportProjectId.value })
      defaultFilename = 'postman-collection.json'
    }

    // 弹出文件保存对话框
    const savePath = await save({
      defaultPath: defaultFilename,
      filters: [{ name: 'JSON', extensions: ['json'] }],
    })

    if (!savePath) {
      loading.value = false
      return false
    }

    await writeTextFile(savePath, content)
    successMsg.value = `已保存到：${savePath}`
    setTimeout(() => { show.value = false }, 2000)
  } catch (e) {
    errorMsg.value = String(e)
  } finally {
    loading.value = false
  }

  return false
}
</script>

<style scoped>
.export-body {
  display: flex;
  flex-direction: column;
  gap: 14px;
  padding: 4px 0;
}
.field-row {
  display: flex;
  align-items: flex-start;
  gap: 12px;
}
.field-label {
  width: 70px;
  font-size: 13px;
  flex-shrink: 0;
  padding-top: 3px;
}
.export-error { font-size: 12px; color: #d03050; }
.export-success { font-size: 12px; color: #18a058; }
</style>
```

### Step 3：vue-tsc 验证

```bash
npx vue-tsc --noEmit
# 期望：无报错
```

### Step 4：提交

```bash
git add src/components/io/ImportDialog.vue src/components/io/ExportDialog.vue
git commit -m "feat(m6): add ImportDialog and ExportDialog components"
```

---

## Task 10：TopBar.vue 集成导入导出入口

**文件：**
- 修改：`src/components/layout/TopBar.vue`

### Step 1：在 script 区引入弹窗

在 import 区追加：
```typescript
import ImportDialog from '../io/ImportDialog.vue'
import ExportDialog from '../io/ExportDialog.vue'

const showImportDialog = ref(false)
const showExportDialog = ref(false)
```

### Step 2：将「⚙️」按钮改为有菜单的下拉

找到：
```html
      <n-button size="small" quaternary title="设置">⚙️</n-button>
```

替换为：
```html
      <n-dropdown
        :options="settingsMenuOptions"
        placement="bottom-end"
        @select="handleSettingsMenu"
      >
        <n-button size="small" quaternary title="更多操作">⚙️</n-button>
      </n-dropdown>
```

### Step 3：在 script 区添加菜单选项和处理函数

```typescript
import { NDropdown } from 'naive-ui'

const settingsMenuOptions = [
  { label: '📥 导入接口...', key: 'import' },
  { label: '📤 导出接口...', key: 'export' },
]

function handleSettingsMenu(key: string) {
  if (key === 'import') showImportDialog.value = true
  else if (key === 'export') showExportDialog.value = true
}
```

### Step 4：在模板末尾（`</template>` 之前）添加弹窗

```html
  <!-- 导入弹窗 -->
  <ImportDialog v-model:show="showImportDialog" />

  <!-- 导出弹窗 -->
  <ExportDialog v-model:show="showExportDialog" />
```

### Step 5：最终全量 vue-tsc 验证

```bash
npx vue-tsc --noEmit
# 期望：无报错
```

### Step 6：最终 cargo check

```powershell
powershell -NonInteractive -Command '
$vsdev = "C:\Program Files\Microsoft Visual Studio\2022\Community\Common7\Tools\VsDevCmd.bat"
$psi = New-Object System.Diagnostics.ProcessStartInfo("cmd.exe")
$psi.Arguments = "/c `"$vsdev`" && cargo check 2>&1"
$psi.UseShellExecute = $false
$psi.RedirectStandardOutput = $true
$psi.RedirectStandardError = $true
$psi.WorkingDirectory = "D:\ProgramFiles\rangeric\apicat\src-tauri"
$p = [System.Diagnostics.Process]::Start($psi)
$out = $p.StandardOutput.ReadToEnd()
$err = $p.StandardError.ReadToEnd()
$p.WaitForExit()
Write-Output $out; Write-Output $err; Write-Output "EXIT: $($p.ExitCode)"
'
# 期望：EXIT: 0
```

### Step 7：提交

```bash
git add src/components/layout/TopBar.vue
git commit -m "feat(m6): integrate import/export entry in TopBar settings menu"
```

---

## 验收标准

| 功能 | 验收条件 |
|------|---------|
| 压测启动 | URL 栏旁出现「⚡ 压测」按钮，点击弹出配置弹窗 |
| 压测配置 | 可设置并发数(1-500)、模式(count/duration)、数值 |
| 压测结果实时更新 | 结果弹窗每 200ms 刷新数字卡片和折线图 |
| 压测完成 | 压测结束后显示「✅ 压测完成」+ 最终统计数据 |
| fd 检测 | macOS/Linux 低 fd 环境下，压测前报错而非崩溃 |
| 导入 Postman v2.1 | 拖入 Postman collection JSON，左侧树出现对应接口 |
| 导入 OpenAPI YAML | 导入 swagger.yaml，接口按 tag 分组出现在左侧树 |
| 导出 ApiCat 格式 | 导出 JSON 文件，重新导入后数据完整一致 |
| 导出 Postman 格式 | 导出后可在 Postman 中正常导入 |
| ⚙️ 菜单入口 | TopBar「⚙️」按钮点击弹出「导入/导出」菜单 |
| vue-tsc 通过 | `npx vue-tsc --noEmit` EXIT: 0 |
| cargo check 通过 | EXIT: 0 |

---

## 执行方式选择

计划已保存至 `docs/plans/2026-03-24-m6-plan.md`。两种执行方式：

**方式 1 — Subagent-Driven（本 session）**：由主 agent 逐 Task 派发 subagent，每 Task 完成后 review，迭代快。需使用 `superpowers:subagent-driven-development`。

**方式 2 — 独立 Session**：新开 session，加载 `superpowers:executing-plans`，按 Task 逐步执行，适合批量执行。

选哪种方式？
